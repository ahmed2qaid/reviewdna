{
  "name": "reviewdna-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Mine code-review history into evidence-backed engineering rules for humans and AI coding agents.",
  "license": "MIT",
  "type": "module",
  "engines": {"node": ">=20"},
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "tsc -b",
    "clean": "find apps packages -type d -name dist -prune -exec rm -rf {} +",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc -b --pretty false",
    "demo": "npm run build && node apps/cli/dist/index.js analyze-fixture fixtures/reviews.json --out demo-output",
    "benchmark": "npm run build && node scripts/benchmark.mjs && node scripts/semantic-benchmark.mjs",
    "benchmark:semantic": "npm run build && node scripts/semantic-benchmark.mjs"
  },
  "devDependencies": {"typescript": "^5.8.3"}
}
