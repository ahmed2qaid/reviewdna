import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import{compareAnalysisResults,discoverRules}from'../packages/core/dist/index.js';import{exportDeltaMarkdown}from'../packages/exporters/dist/index.js';test('delta markdown summarizes convention changes',async()=>{const fixture=JSON.parse(await readFile(new URL('../fixtures/reviews.json',import.meta.url),'utf8')),before=discoverRules(fixture.slice(0,6),'acme/backend','fixture'),after=discoverRules(fixture,'acme/backend','fixture'),text=exportDeltaMarkdown(compareAnalysisResults(before,after),'acme/backend');assert.match(text,/New conventions/);assert.match(text,/regression test/i);});
