import type { ReviewRecord } from '@reviewdna/schema';
export interface GitHubCollectorOptions { token?:string|undefined; maxPullRequests?:number|undefined; includeIssueComments?:boolean|undefined; }
interface GitHubPR {number:number;title:string;html_url:string;user?:{login?:string};merged_at?:string|null} interface GHReviewComment{id:number;body?:string;html_url:string;user?:{login?:string};created_at:string;path?:string} interface GHIssueComment{id:number;body?:string;html_url:string;user?:{login?:string};created_at:string} interface GHReview{id:number;body?:string;html_url:string;user?:{login?:string};submitted_at?:string;state?:string}
export class GitHubCollector { private readonly token:string|undefined; private readonly maxPullRequests:number; private readonly includeIssueComments:boolean; constructor(o:GitHubCollectorOptions={}){this.token=o.token;this.maxPullRequests=o.maxPullRequests??100;this.includeIssueComments=o.includeIssueComments??false;} private headers(){return {Accept:'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28',...(this.token?{Authorization:`Bearer ${this.token}`}:{})};} private async get<T>(url:string):Promise<T>{const res=await fetch(url,{headers:this.headers()});if(!res.ok){const remain=res.headers.get('x-ratelimit-remaining'),reset=res.headers.get('x-ratelimit-reset');throw new Error(`GitHub API ${res.status}: ${await res.text()}${remain==='0'?`\nRate limit reached. Reset epoch: ${reset}`:''}`);}return res.json() as Promise<T>;} private async paged<T>(url:string,limit=1000):Promise<T[]>{const out:T[]=[];for(let page=1;out.length<limit;page++){const join=url.includes('?')?'&':'?';const batch=await this.get<T[]>(`${url}${join}per_page=100&page=${page}`);out.push(...batch);if(batch.length<100)break;}return out.slice(0,limit);} async collect(repository:string):Promise<ReviewRecord[]>{const [owner,repo]=repository.split('/');if(!owner||!repo)throw new Error('Repository must be in owner/name form.');const pulls=await this.paged<GitHubPR>(`https://api.github.com/repos/${owner}/${repo}/pulls?state=closed&sort=updated&direction=desc`,this.maxPullRequests),merged=pulls.filter(p=>p.merged_at).slice(0,this.maxPullRequests),records:ReviewRecord[]=[];let position=0;for(const pr of merged){position++;process.stderr.write(`\rCollecting reviews ${position}/${merged.length} (PR #${pr.number})`);const [comments,reviews,issueComments]=await Promise.all([this.paged<GHReviewComment>(`https://api.github.com/repos/${owner}/${repo}/pulls/${pr.number}/comments`,500),this.paged<GHReview>(`https://api.github.com/repos/${owner}/${repo}/pulls/${pr.number}/reviews`,200),this.includeIssueComments?this.paged<GHIssueComment>(`https://api.github.com/repos/${owner}/${repo}/issues/${pr.number}/comments`,300):Promise.resolve([])]);for(const c of comments)if(c.body?.trim())records.push({id:`rc-${c.id}`,repo:repository,prNumber:pr.number,prTitle:pr.title,author:pr.user?.login,reviewer:c.user?.login??'unknown',body:c.body,path:c.path,createdAt:c.created_at,url:c.html_url,source:'review-comment'});for(const r of reviews)if(r.body?.trim())records.push({id:`rv-${r.id}`,repo:repository,prNumber:pr.number,prTitle:pr.title,author:pr.user?.login,reviewer:r.user?.login??'unknown',body:r.body,createdAt:r.submitted_at??new Date().toISOString(),url:r.html_url,accepted:r.state==='APPROVED',source:'review'});for(const c of issueComments)if(c.body?.trim())records.push({id:`ic-${c.id}`,repo:repository,prNumber:pr.number,prTitle:pr.title,author:pr.user?.login,reviewer:c.user?.login??'unknown',body:c.body,createdAt:c.created_at,url:c.html_url,source:'issue-comment'});}process.stderr.write('\n');return records;} }
