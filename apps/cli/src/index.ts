#!/usr/bin/env node
import {mkdir,readFile,writeFile} from 'node:fs/promises';import {resolve} from 'node:path';import {discoverRules} from '@reviewdna/core';import {GitHubCollector} from '@reviewdna/github';import {exportAgents,exportClaude,exportCursor,exportMarkdown} from '@reviewdna/exporters';import {renderHtml} from '@reviewdna/report';import type {AnalysisResult,ReviewRecord} from '@reviewdna/schema';
const VERSION='0.1.0';const flag=(args:string[],name:string,fallback?:string)=>{const i=args.indexOf(name);return i>=0?args[i+1]:fallback};const has=(args:string[],name:string)=>args.includes(name);function help(){console.log(`ReviewDNA ${VERSION}\n\nYour reviews already contain your engineering DNA.\n\nCommands:\n  reviewdna analyze owner/repo [--max-prs 100] [--out reviewdna-output] [--issue-comments]\n  reviewdna analyze-fixture file.json [--out reviewdna-output]\n  reviewdna export result.json --format agents|claude|cursor|markdown\n  reviewdna doctor\n  reviewdna --version\n`)} async function save(result:AnalysisResult,outDir:string){await mkdir(outDir,{recursive:true});await Promise.all([writeFile(resolve(outDir,'reviewdna.json'),JSON.stringify(result,null,2)),writeFile(resolve(outDir,'reviewdna-report.html'),renderHtml(result)),writeFile(resolve(outDir,'engineering-dna.md'),exportMarkdown(result)),writeFile(resolve(outDir,'AGENTS.suggested.md'),exportAgents(result)),writeFile(resolve(outDir,'CLAUDE.suggested.md'),exportClaude(result)),writeFile(resolve(outDir,'cursor.suggested.mdc'),exportCursor(result))]);} function summary(r:AnalysisResult){console.log(`\nReviewDNA 🧬\n\nRepository: ${r.summary.repository}\nReviews analyzed: ${r.summary.reviewsAnalyzed}\nPull requests: ${r.summary.pullRequests}\nReviewers: ${r.summary.reviewers}\nRules discovered: ${r.summary.rules}\nHigh-confidence: ${r.summary.highConfidenceRules}\nConflicting: ${r.summary.conflictingRules}\nStale: ${r.summary.staleRules}\n`)} async function main(){const args=process.argv.slice(2),cmd=args[0];if(!cmd||has(args,'--help')||cmd==='help'){help();return}if(has(args,'--version')||cmd==='--version'){console.log(VERSION);return}if(cmd==='doctor'){console.log(`ReviewDNA doctor\nNode: ${process.version} ✓\nGitHub token: ${process.env.GITHUB_TOKEN?'present ✓':'not set (public anonymous mode)'}\nRuntime fetch: ${typeof fetch==='function'?'available ✓':'missing ✗'}\n`);return}if(cmd==='analyze'){const repo=args[1];if(!repo)throw new Error('Missing repository. Example: reviewdna analyze owner/repo');const max=Number(flag(args,'--max-prs','100')),out=flag(args,'--out','reviewdna-output')!;const collector=new GitHubCollector({token:process.env.GITHUB_TOKEN,maxPullRequests:max,includeIssueComments:has(args,'--issue-comments')});const records=await collector.collect(repo),result=discoverRules(records,repo,'github');await save(result,out);summary(result);console.log(`Output: ${resolve(out)}\nOpen reviewdna-report.html in your browser.`);return}if(cmd==='analyze-fixture'){const file=args[1];if(!file)throw new Error('Missing fixture JSON path.');const out=flag(args,'--out','reviewdna-output')!,records=JSON.parse(await readFile(file,'utf8')) as ReviewRecord[],repo=records[0]?.repo??'fixture/repository',result=discoverRules(records,repo,'fixture');await save(result,out);summary(result);return}if(cmd==='export'){const file=args[1];if(!file)throw new Error('Missing reviewdna.json path.');const format=flag(args,'--format','agents')!,result=JSON.parse(await readFile(file,'utf8')) as AnalysisResult,exporters:Record<string,(r:AnalysisResult)=>string>={agents:exportAgents,claude:exportClaude,cursor:exportCursor,markdown:exportMarkdown},fn=exporters[format];if(!fn)throw new Error(`Unknown format: ${format}`);process.stdout.write(fn(result));return}throw new Error(`Unknown command: ${cmd}`)}main().catch(err=>{console.error(`\nReviewDNA error: ${err instanceof Error?err.message:String(err)}`);process.exitCode=1});
